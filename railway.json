{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "python3 manage.py flush --no-input && python3 manage.py migrate --noinput && python3 manage.py createsuperuser --noinput || true && python3 manage.py collectstatic --noinput && python3 -m gunicorn qr_inventory_project.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
